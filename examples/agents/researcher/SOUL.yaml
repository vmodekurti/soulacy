id: researcher
name: Researcher (DEPRECATED)
description: |
  DEPRECATED — replaced by `kb-researcher`. This agent is kept here only so
  any existing chat sessions don't crash; delete it from the Agents page
  whenever you're ready. Use kb-researcher for KB-grounded research and
  web-researcher for current-events queries.
version: "1.0.0-deprecated"
tags: [deprecated]

trigger: channel
channels: [http]

system_prompt: |
  This agent has been deprecated. Please use `kb-researcher` instead.

llm:
  provider: ollama
  model: qwen2.5:72b
  temperature: 0
  max_tokens: 128

memory:
  read_scopes: [session]
  write_scopes: [session]
  max_tokens: 5

max_turns: 1
enabled: false
